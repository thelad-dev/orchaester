# Models

Fill after the first week. Example shape:

- grok-fast — trivial, cheap, misses cross-file intent
- cursor/grok — standard ship when the index is warm
- claude — hard review when Cursor quota is gone
- local/qwen — RLM workers, never ship-of-record unless captain says so
